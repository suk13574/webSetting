<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.devfun.settingweb_boot.dao.StatisticList">

	<select id="selectMonth" parameterType="string" resultType="com.devfun.settingweb_boot.dto.LoginUser">
		select ri.userId as userId,  user.HR_ORGAN as hrOrgan, ri.createDate as createDate
		from statistic.requestinfo ri
		join statistic.user user
			on ri.UserID = user.userID
		where mid(ri.createDate, 3, 2) = #{month}
			AND ri.requestCode = 'L';
	</select>
	
	<select id="selectDay" parameterType="string" resultType="com.devfun.settingweb_boot.dto.LoginUser">
		select ri.userId as userId,  user.HR_ORGAN as hrOrgan, ri.createDate as createDate
		from statistic.requestinfo ri
		join statistic.user user
			on ri.UserID = user.userID
		where mid(createDate, 5, 2) = #{day}
			AND ri.requestCode = 'L';
	</select>
	
	<select id="selectMonthDepartment" parameterType="string" resultType="com.devfun.settingweb_boot.dto.MonthCnt">
		select count(*) as cnt, mid(ri.createDate, 3, 2) as month
		from statistic.requestinfo ri
		join statistic.user user
			on ri.UserID = user.userID
		where ri.requestCode = 'L'
			AND user.HR_ORGAN = #{department}
		group by mid(ri.createDate, 3, 2);
	</select>
</mapper>

